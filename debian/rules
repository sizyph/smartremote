#!/usr/bin/make -f
# Build the SmartRemote Debian package via dh-python/pybuild.
export PYBUILD_NAME=smartremote

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_test:
	# Unit tests run in CI (.github/workflows/ci.yml); skip during packaging.
